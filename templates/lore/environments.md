---
doc: environments
title: Environments & config
last-verified: {{DATE}}
read-when: touching configuration, env vars, or anything environment-specific
update-when: an env var is added/removed or an environment URL changes
---

# Environments & config

NAMES ONLY — never write secret values in this file or anywhere in the repo.

## Environments
| Env | URL / location | Notes |
| --- | --- | --- |
| local | _FILL_ME_ | |
| production | _FILL_ME_ | |

## Environment variables
| Variable | Purpose | Required in |
| --- | --- | --- |
| _FILL_ME_ | _FILL_ME_ | all |

## Secrets policy
- Secrets live in: _FILL_ME_ (e.g. `.env` (gitignored), 1Password, cloud secret manager)
- Agents: if a needed secret is missing, add a request to user-actions.md —
  never invent, hardcode, or commit one.
