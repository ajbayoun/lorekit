---
doc: setup
title: Setup & install
last-verified: {{DATE}}
read-when: working in a fresh environment; anything fails with "not found" errors
update-when: prerequisites, install steps, or run commands change
---

# Setup & install

From zero to running locally. Keep this executable top-to-bottom — if a step
breaks, fixing this file is part of fixing the problem.

## Prerequisites
- _FILL_ME_ (runtime + version)

## Install
```
_FILL_ME_
```

## Run
```
_FILL_ME_   # dev
_FILL_ME_   # production build
```

## First-run gotchas
- _FILL_ME_ (or "none")
