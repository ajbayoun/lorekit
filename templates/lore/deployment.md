---
doc: deployment
title: Deployment
last-verified: {{DATE}}
read-when: touching Docker, Kubernetes, CI/CD, or anything that ships to a server
update-when: infrastructure, images, manifests, or the deploy path change
---

# Deployment

## Where this runs
_FILL_ME_ (e.g. "Docker Compose on a VPS", "EKS cluster", "Vercel")

## Build & deploy
```
_FILL_ME_   # build the image / artifact
_FILL_ME_   # deploy
```

## Docker / Kubernetes notes
- Images: _FILL_ME_ (registry, tagging scheme)
- Manifests / compose files live in: _FILL_ME_
- _FILL_ME_ (resource limits, health checks, anything non-obvious)

## Rollback
_FILL_ME_ (the exact steps — write them BEFORE you need them)
