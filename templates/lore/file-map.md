---
doc: file-map
title: File map
last-verified: {{DATE}}
read-when: first session in the repo; deciding where new code belongs
update-when: top-level directories are added, removed, or repurposed
---

# File map

Annotated directory tree — what lives where and what's off-limits.
Only top-level and load-bearing paths; this is a map, not an inventory.

```
_FILL_ME_/          what it holds, what belongs there
```

## Placement rules
- New _FILL_ME_ go in `_FILL_ME_`
- Never put _FILL_ME_ in `_FILL_ME_`
