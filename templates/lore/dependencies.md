---
doc: dependencies
title: Dependencies
summary: Key libraries, why chosen, and upgrade policy
last-verified: {{DATE}}
read-when: adding, upgrading, or replacing a library
update-when: a key dependency is added or removed — record the WHY
---

# Dependencies

Key libraries and WHY each was chosen. Agents: do not swap or add major
dependencies without checking decisions.md and asking — "there's a newer
library" is not a reason.

| Dependency | Why this one | Notes / constraints |
| --- | --- | --- |
| _FILL_ME_ | _FILL_ME_ | _FILL_ME_ |

## Upgrade policy
_FILL_ME_ (e.g. "patch/minor freely if tests pass; major versions need a decision entry")

## Banned
- _FILL_ME_ (libraries tried and rejected — link to experiments.md; or "none")
